# Runbooks publicados

Os playbooks aprovados pelo Hub aparecem abaixo de
`docs/runbooks/<ano>/<domain>/<nome>--<job_id>.md`. O domínio vem da identidade
validada pelo Hub, não do conteúdo enviado pelo CLI. Esta página é mantida no repositório para
que a seção exista antes da primeira publicação.
