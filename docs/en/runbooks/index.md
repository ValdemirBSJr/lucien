# Published runbooks

The playbooks approved by the Hub appear under
`docs/runbooks/<year>/<domain>/<name>--<job_id>.md`. The domain comes from the
identity the Hub validated, not from the content the CLI sent. This page is kept
in the repository so that the section exists before the first publication.
